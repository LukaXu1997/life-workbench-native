# V2.14.16 发布闭环 — 执行与状态报告

> 执行日期：2026-09-03
> 范围：仅完成 V2.14.16 发布闭环的准备、CI 工作流加固与推送尝试。**未升版本号、未重打 Tag、未改业务源码、未提交 TnG/导入适配器改动。**
> 原则：未执行的构建 / 验签 / 设备测试一律如实标注为「未执行」，绝不伪造。

---

## 1. 开始前只读检查（step 一）— 全部一致 ✅

| 检查项 | 结果 |
|---|---|
| 项目身份 | `/Users/Luka/WorkBuddy/life-workbench-native`，`package.json` `name = "life-workbench-native"` ✅ |
| `git status --short` | TnG/导入适配器改动仍 **未提交、未暂存**（见第 8 节清单）✅ |
| `git log -10` | `743f8e3 (HEAD -> main)` → `9f8ee61 (tag: v2.14.16, origin/main, origin/HEAD)` → `df217d8` … ✅ |
| `git show --stat 9f8ee61` | 17 文件，纯安全加固 + 2 bug 修复 + 版本号（无 workflow）✅ |
| `git show --stat 743f8e3` | 仅 `.github/workflows/release-android.yml`（97 行）✅ |
| `git tag --points-at 9f8ee61` | `v2.14.16` ✅ |
| `git ls-remote origin` | `refs/heads/main → 9f8ee61`；`refs/tags/v2.14.16 → 9f8ee61`；`refs/tags/v2.14.15 → f91ad98` ✅ |

**结论**：`v2.14.16` 正确指向安全修复提交；`743f8e3` 仅含 CI 工作流；工作区原有未提交改动完好，**全部与已知状态一致，未擅自修历史**。

---

## 2. CI 工作流审查与加固（step 三）— 已落地

原始 `743f8e3` 的工作流缺少若干门禁项。按指令**仅修改该工作流文件**，新建独立修复提交 `f0cbeb2`（未 amend 已推送的业务提交 `9f8ee61`）。

补全的门禁：
- keystore 解码到 `$RUNNER_TEMP`（不再落仓库树），任务末 `rm -f` 删除（双保险，绝不进产物/日志）；
- `apksigner verify --verbose --print-certs`；
- 证书 DN 同时匹配 `Android Debug` **或** `androiddebugkey` 即失败；
- 上传产物改名 `life-workbench-<version>-release.apk`（版本号取自 `build.gradle` 的 `versionName`）；
- 安装 NDK `26.1.10909125`（RN 0.76 新架构构建必需，否则 `assembleRelease` 会失败）；
- 保留：`v*.*.*` tag 触发 + `workflow_dispatch`、`setup-java` JDK 17、`npm ci`、`npm run verify` 门禁、4 个 `MYAPP_RELEASE_*` 注入、仅上传 APK。

YAML 已用 pyyaml 校验合法（`on:` 被解析为布尔键属正常，GitHub 可识别）。

---

## 3. 推送 CI 工作流（step 二）— ❌ 被拒，真实阻塞

```
$ git push origin main
! [remote rejected] main -> main
  (refusing to allow a Personal Access Token to create or update workflow
   `.github/workflows/release-android.yml` without `workflow` scope)
```

- 本环境 git 凭据为 **PAT，缺 `workflow` OAuth scope**；`gh` CLI 未安装，无法用 `gh auth refresh -s workflow`。
- **此权限只能由你通过正常 GitHub 授权流程补充**（换新 PAT 并勾选 `workflow`，或在 GitHub 账户/令牌设置里为该 PAT 加 scope）。
- 我**未打印、未写入任何 Token / 密码 / keystore**；**未 force push、未新建 Tag、未改动或重指 `v2.14.16`**。

### 当前远程状态
- `origin/main` = `9f8ee61`（安全 release，已上线）✅
- 本地 `main` = `f0cbeb2`，领先 origin **2 个提交**：`743f8e3`（CI v1）+ `f0cbeb2`（CI 加固）。二者**仅含工作流文件**。
- `tag v2.14.16` 已在 origin，未动。

---

## 4. GitHub Actions Secrets（step 四）— 需你配置（我不读取/不生成）

工作流依赖以下 **4 个仓库 Actions Secrets**（仅列名称，未读取值）：

1. `MYAPP_RELEASE_STORE_FILE_B64` — 正式 `.keystore` 的 base64（`base64 -w0 release.keystore`）
2. `MYAPP_RELEASE_KEY_ALIAS`
3. `MYAPP_RELEASE_STORE_PASSWORD`
4. `MYAPP_RELEASE_KEY_PASSWORD`

⚠️ **关键约束**：若你已有历史正式签名 keystore，**必须使用同一把密钥**。不能用新密钥代替，否则旧版本可能无法覆盖升级（用户数据丢失风险）。**未经你明确授权，我不会生成任何新 keystore。**

---

## 5 / 6 / 7. 运行工作流 / APK 验收 / Pixel 回归 — ⛔ 未执行

以下三项**因推送阻塞 + Secrets 未就绪而无法执行**。任何 APK 文件、签名摘要、证书 SHA-256、Pixel 覆盖安装结果**均不存在**，本报告中**不虚构**。

待你完成：①补 `workflow` scope 并允许我推送；②配置上述 4 个 Secrets（历史 keystore）后，我将：
- 通过 `workflow_dispatch` 在 `main`（HEAD 含 9f8ee61 源码，versionName 2.14.16）触发；
- 等待完成，失败则只修 CI/构建问题，不绕过 `npm run verify`、签名断言或测试，不用 `continue-on-error`；
- 下载产物 APK，验收：applicationId `com.luka.lifeworkbench`、versionName `2.14.16`、versionCode `21416`、minSdk 24 / targetSdk 35、签名有效且非 Android Debug、证书 SHA-256 与历史正式版一致、Manifest 无新增高风险 exported/权限，并记录 APK SHA-256；
- 仅在验签通过后，对 Pixel `46201FDAP007FD` **覆盖安装（不卸载）**，执行第 7 节全部回归项。

---

## 8. 最终报告清单（当前真实状态）

- **已推送提交**：`9f8ee61`（v2.14.16 安全 release）+ tag `v2.14.16`，均在 origin ✅
- **未推送提交**：`743f8e3`、`f0cbeb2`（仅 CI 工作流，待 `workflow` scope 后推送）
- **CI 工作流状态**：已写好并加固，YAML 合法，但因 scope 未推送 → 尚未在 GitHub 运行
- **每项测试实际结果**：`npm run verify`（tsc 0 错 / notify 全绿 / security gate PASS）此前已在本地全绿；CI 内重跑尚未发生
- **APK 文件名 / 大小 / SHA-256**：未生成（未执行）
- **applicationId / versionName / versionCode**：源码固定为 `com.luka.lifeworkbench` / `2.14.16` / `21416`（取自 `build.gradle` / `version.ts`，未改）
- **签名方案与证书 SHA-256**：未执行，无数据
- **与历史正式证书是否一致**：待验（需 Secrets 就绪后比对）
- **Pixel 覆盖安装结果**：未执行
- **数据保留结果**：未执行
- **未完成项 / 真实阻塞**：
  1. PAT 缺 `workflow` scope → 无法推送工作流（你补充 scope 即可）；
  2. 仓库 Actions Secrets 未配置（需你填入历史 keystore 的 4 项）；
  3. 本环境无 JDK/SDK，但 CI 在 GitHub 云端跑，不依赖本机；
  4. 步骤 5/6/7 全部依赖上述两项就绪。
- **工作区原有未提交改动是否保持原样**：✅ 完全保持，未暂存/未混入/未删除

### 工作区未提交改动清单（保持原样）
修改（已跟踪，未 `git add`）：
`scripts/import-test-runner.js`、`src/import/accountResolver.ts`、`src/import/models.ts`、`src/import/runImport.ts`、`src/import/schemas.ts`、`src/import/sourceDetect.ts`、`src/screens/ImportFlowModal.tsx`

未跟踪：
`src/import/__phase9_tests.ts`、`src/import/adapters/myrEwalletCsv.ts`、`.tmptest_tng/`、`SECURITY_VERIFICATION_REPORT.md`、`V2.14.0-TnG-Capture-Overview.md`

---

## 待你决策 / 操作
1. 给本机 git PAT 增加 `workflow` scope（或换带 `workflow` 的新 PAT）；
2. 在仓库 **Settings → Secrets and variables → Actions** 配置 4 个 Secrets（使用**历史正式 keystore**）；
3. 回复确认后，我即推送 `743f8e3`+`f0cbeb2` 并通过 `workflow_dispatch` 触发构建，继续 step 5–8。
