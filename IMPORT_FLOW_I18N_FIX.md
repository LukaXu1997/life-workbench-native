# 导入预览行数不显示 — 修复记录

## 问题
导入流水预览只显示字面 `共 {n} 行，可导入 {n} 笔`，数字（行数/可导入笔数）始终不渲染。

## 根因
`src/i18n/zh.ts` 与 `en.ts` 的 `importFlow` 文案使用了**单花括号**占位符 `{n}` / `{name}`。
而项目使用的 `i18n-js@4.5.3` 的插值正则仅为 `/(?:\{\{|%\{)(.*?)(?:\}\}?)/gm` —— 只识别 `{{n}}` 或 `%{n}`，**不识别单 `{n}`**。
全项目其它所有文案都用 `%{...}`，只有 `importFlow` 这一段是例外，导致导入相关 9 个文案（含重复/疑似/跨币/未匹配/密码错误尝试等）的变量全部未替换。

## 修复
将两文件中的 9 个 `importFlow` 文案占位符统一改为 `%{...}`：
- `rowsTotal` `importable` `duplicates` `suspected` `crossCurrency` `unassigned` `commit` → `{n}`→`%{n}`
- `pwdFile` → `{name}`→`%{name}`；`pwdAttempts` → `{n}`→`%{n}`

## 验证
- 用真实 `i18n-js` 跑插值：`共 %{n} 行` + `{n:492}` → `共 492 行` ✅；旧 `{n}` 仍输出字面 `共 {n} 行` ❌（即原 bug）。
- 编译后的 bundle 检查：旧 `{n}` 出现 0 次，新 `%{n}` 存在 ✅。
- `tsc --noEmit` 干净通过。
- 重新打包签名 APK（v1.2.2 / 10202，SHA-256 `d09b08c2…04200764`）并已 `adb install -r` 安装到 Pixel 9 Pro（数据保留）。

## 受影响文案（修复后示例）
- 共 492 行 · 可导入 480 笔
- 跳过重复 8 笔 / 疑似重复 2 笔 / 跨币合并 1 对 / 12 笔未匹配到账户
- 导入 480 笔
