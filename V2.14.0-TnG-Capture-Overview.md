# V2.14.0 — TnG 付款实时捕获

## 问题
Touch 'n Go 付款成功**不推送任何系统通知**，导致既有的「通知监听 → 待确认 → 确认」记账管线永远收不到 TnG 交易，无法实现「付款后立即统计支出」。

## 方案（混合：无障碍为主 + OCR 兜底）
两条新路线都生成与通知**完全同构的 `NotifyEnvelope`**，复用既有的 `NotifyBridge.emit` → 同一持久队列 → `parseEnvelope` / `recognize` / `dedup` / `ingestEnvelope` → 待确认列表 → 确认入账。**零重复解析/去重逻辑，不自动入账、不发裸通知。**

| 路线 | 实现 | 触发 |
|---|---|---|
| 无障碍（主） | `TxnCaptureService.kt`（AccessibilityService）遍历付款成功页**文字节点** | 命中 `成功关键词(Berjaya/success/支付成功…) + RM xx.xx` 才捕获，含同屏去重 |
| OCR（兜底） | `OcrCapture.kt` 用 ML Kit `text-recognition:16.0.1`（纯端侧、无网络） | 用户在系统分享里把 TnG 截图**分享到本 App** |

## 隐私边界（显式、有意）
- 仅读文字节点，不截图、不读剪贴板、不上传；仅对你勾选的 App 生效。
- 屏幕/截图文本只写入 App 私有临时队列（JS 入账后即清空），不持久化原始文本。

## 关键文件
- 新增 `android/.../notify/TxnCaptureService.kt`、`android/.../quickadd/OcrCapture.kt`、`res/xml/txn_capture_service.xml`
- 改 `NotifyConfig.kt`（新增 `captureEnabled`/`captureAllowSet`/`isCaptureAllowed`，与通知监听解耦）、`NotifyModule.kt`（`isTxnCaptureEnabled`/`openAccessibilitySettings`）、`NotifyListener.kt`（头部注释）、`AndroidManifest.xml`（注册无障碍服务 + SEND 增加 `image/*`）、`QuickAddBridge.kt`（分享截图触发 OCR）、`build.gradle`（ML Kit 依赖 + 版本 21400）
- JS：`types.ts`（`tngCapture?`）、`pendingStore.ts`、`NativeNotifyModule.ts`、`NotifySettingsTab.tsx`（新增独立开关 + 无障碍权限状态/跳转）、`i18n`（6 条）、`App.tsx`（冷启动下发配置）、`version.ts`/`app.json`/`CHANGELOG.md`

## 验证
- ✅ `assembleRelease` BUILD SUCCESSFUL；versionCode **21400** / versionName **2.14.0**
- ✅ aapt 确认 `TxnCaptureService` 已注册（`BIND_ACCESSIBILITY_SERVICE` + AccessibilityService intent-filter + meta-data）
- ✅ `scripts/tng_capture_smoke.js`（纯 Node 转译核心管线）注入 accessibility + ocr 信封 → 均生成待确认记录（RM 12.50 / RM 8.00，MYR），无金额正确跳过；3/3 PASS
- ⚠️ **真机安装/验收本会话未完成**：`adb devices` 为空（Pixel 未连接）。待重连后执行：

```bash
adb -s 46201FDAP007FD install -r /Users/Luka/Downloads/app-release.apk
# 系统设置 → 无障碍 → 开启「生活工作台」
# App → 我的 → 付款后自动记账 → 开启「TnG 付款实时捕获」→ 点「前往系统设置授予无障碍权限」
# 用 TnG 付一笔 → 待确认列表应出现该笔
```
注：`adb install -r` 保留数据，切勿 `uninstall`。
