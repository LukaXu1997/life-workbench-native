# V3.0.2 发布报告（life-workbench-native）

发布日期：2026-09-03
包名：`com.luka.lifeworkbench`
版本：`3.0.2` / `versionCode 30002`
构建产物：`life-workbench-3.0.2-release.apk`（51.3 MB）

---

## 1. 版本升级（单一来源，4 个文件）

| 文件 | 改动 |
|---|---|
| `src/version.ts` | `PATCH 1 → 2`（自动派生 `VERSION_NAME "3.0.2"`、`VERSION_CODE 30002`）；新增 V3.0.2 RELEASE_NOTES 条目 |
| `android/app/build.gradle` | `versionCode 30001 → 30002`；`versionName "3.0.1" → "3.0.2"` |
| `app.json` | `version "2.14.15" → "3.0.2"`；`android.versionCode 21415 → 30002` |
| `CHANGELOG.md` | 顶部新增 `## V3.0.2 — 2026-09-03` 条目 |

> V3.0.2 与 V3.0.1 **功能内容完全一致**（安全加固发布版，SCHEMA_VERSION 仍为 2，无存储结构 / 导航 / 功能逻辑改动）。本次升版的主要目的：以正确指向的 `v3.0.2` 标签重建正式发布产物，修正此前 `v3.0.1` 标签未指向 main 最新提交（`3da1300` ≠ `5a94771`）的问题，建立可追溯的标签化发布。
>
> **未提交项（按既有约定保留）**：Import / TnG 相关 7 个已修改文件 + 若干未跟踪文件（报告、测试、`release-artifacts/`、`src/import/adapters/myrEwalletCsv.ts` 等）在本提交中**刻意排除**，仅提交上述 4 个版本文件。

## 2. 提交与标签

- 提交 `5980802`（`release: V3.0.2 / versionCode 30002 (version bump only)`），仅含 4 个版本文件，+21 / −6。
- 标签 `v3.0.2`（annotated）指向 `5980802`。
- `main` 与 `v3.0.2` 均已推送到 origin。
- `v3.0.1` 标签**未改动**，仍指向 `3da1300`（遵守“不移动/重打旧标签”约束）。

## 3. CI 重建（GitHub Actions `release-android.yml`）

- 触发：推送 `v3.0.2` 标签（`on: push: tags: ['v*.*.*']`）。
- Run **33740839494**，结论 **success**。
- 硬性门禁（构建前）：`npm run verify`（tsc `--noEmit` + 全量测试 + 静态安全门禁）通过。日志可见 `PASS TnG / Maybank / Alipay / WeChat / rec TnG account` 等用例。
- 构建后：`apksigner verify` 确认**非 Debug 证书**（拒绝发布 Debug 签名产物）。
- 产物：APK 重命名为 `life-workbench-3.0.2-release.apk`，以 artifact `life-workbench-release-3.0.2` 上传。

## 4. APK 验收（androguard 解析，无 Java 运行环境下）

| 检查项 | 结果 |
|---|---|
| applicationId | `com.luka.lifeworkbench` ✅ |
| versionName / versionCode | `3.0.2` / `30002` ✅ |
| 已签名 | True ✅ |
| 签名证书主体 | `CN=Luka, OU=LifeWorkbench, O=Personal, C=CN` |
| 证书 SHA-256 | `148048562d94257d42c6899cfb040bbebef19ed4f7f1552441785266e0dcbceb` |
| 与历史正式证书一致 | ✅ 一致（与 V3.0.1 构建及历史正式基线相同） |
| 非 Debug 证书 | ✅（`fac61745…1033b9c` 为 Debug，未命中） |
| 请求权限 | 29 项，均为常规（含 expo-notifications 的 launcher/badge 权限），无新增高风险项 |
| APK 文件 SHA-256 | `c0ac5540ea6958c952480f2c4a500968dc377d519e7b9a81dd49272f233473de` |

**结论：PASS — 签名与历史正式证书一致，Manifest 正常。**

## 5. 数据备份与安装

- **本地 `adb backup`（Android 15 限制）**：执行 `adb backup -f app-data-backup.ab -noapk com.luka.lifeworkbench` 返回 exit 0，但仅生成 **47 字节头文件**（无数据条目）。原因：Android 12+ 对非 debuggable 的正式发布包禁用 `adb backup` 数据导出。即本地备份**实际为空**，未捕获数据。
- **首轮 `adb install -r` 失败**：`INSTALL_FAILED_UPDATE_INCOMPATIBLE — Existing package signatures do not match`。根因：设备上原有 **debug 签名**的 `2.14.15`（`fac61745…1033b9c`，`CN=Android Debug`），与官方发布证书 `14804856…e0dcbceb` 不一致，`install -r` 要求同签名。
- **用户决策**：授权执行 `adb uninstall` 后重装（接受本地数据清除，覆盖“禁止 uninstall”保护）。
  - `adb uninstall com.luka.lifeworkbench` → `Success`
  - `adb install life-workbench-3.0.2-release.apk` → `Success`
- **装后复核**：设备上现为 `versionName=3.0.2 / versionCode=30002 / arm64-v8a`，签名证书 SHA-256 `14804856…e0dcbceb`，与官方正式证书完全一致 ✅。

> ⚠️ 数据提示：卸载清除了 debug 版 `2.14.15` 的本地数据。若 App 已开启 Supabase 云同步，重装登录后可从云端恢复；未同步的本地数据已不可恢复。后续升级因同用官方证书，`adb install -r` 可正常使用（保留数据）。

## 6. 交付物位置

```
release-artifacts/v3.0.2/
├── life-workbench-3.0.2-release.apk   # 正式发布 APK（51.3 MB）
├── build-logs.zip                    # CI 全量构建日志
├── app-data-backup.ab                # 本地备份尝试产物（仅 47 字节头，无数据，见 §5）
└── V3.0.2_RELEASE_REPORT.md          # 本报告
```

## 7. 后续建议

1. 确认 Supabase 云同步状态，必要时在 App 内触发一次备份/恢复，确保数据回归。
2. 真机启动冒烟测试：启动无 FATAL/crash；记账/任务/习惯/日记/设置各模块可用；底部导航与 Home 四宫格正常。
3. 后续补丁发布（如 V3.0.3）可直接 `adb install -r`（同官方证书），无需再次卸载。
