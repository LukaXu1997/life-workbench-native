# V1.2.3 交付报告 — 双币种月度预算卡片重设计 + 版本升级

**状态**：代码完成、类型检查通过、全部逻辑单测绿；**未构建 / 未安装 APK**（按用户要求：待视觉与测试结果确认后再构建 V1.2.3）。

**工作目录**：`/Users/Luka/WorkBuddy/life-workbench-native/`（包名 `com.luka.lifeworkbench`）
> ⚠️ 注意：本项目实际工作副本在 `life-workbench-native/`，而非裸 fork 的 `2026-08-10-11-43-36/TofuFit/`（后者是初始脚手架，无 `FinanceScreen`/`version.ts`/导入系统，请勿混淆）。

---

## 1. 修改文件清单

| 文件 | 改动类型 | 说明 |
|---|---|---|
| `src/screens/FinanceScreen.tsx` | 重写 | 新增 `BudgetCard` / `MonthNav`（重排）/ `FinanceTabBar`（横向滚动）；概览页预算区复用 `BudgetCard`；预算页 `BudgetTab` 接入币种切换 |
| `src/components/ui.tsx` | 加固 | `Segmented` 改为 `width:'100%'` + 子项 `flex:1, minWidth:0, flexShrink:1, numberOfLines:1`（修复溢出根因）；`ProgressIndicator` 高度 6dp / 胶囊圆角 / `clampPct` 封顶 |
| `src/i18n/zh.ts` | 新增/缩短 | `budgetNotSetCur` / `monthRemain` / `usedLabel` / `totalBudgetLabel` / `overBudgetLabel`；顶部分页短名 `概览/预算/趋势` |
| `src/i18n/en.ts` | 新增/缩短 | 对应英文；`segCredit:'Cards'`；顶部分页短名 |
| `src/version.ts` | 升级 | `PATCH:3` → `VERSION_NAME 1.2.3` / `DISPLAY_VERSION V1.2.3` / `VERSION_CODE 10203` / `BUILD_DATE 2026-08-29` / `RELEASE_NOTES` 重写 |
| `package.json` | 升级 | `version: "1.2.3"` |
| `app.json` | 升级 | `version: "1.2.3"`，`android.versionCode: 10203` |
| `android/app/build.gradle` | 升级 | `versionCode 10203` / `versionName "1.2.3"` |
| `src/import/__phase8_tests.ts` | 新增 | 26 条双币种预算规则测试（§九 / §十二 #10–14） |
| `scripts/import-test-runner.js` | 注册 | 注册 `__phase8_tests.ts` 到 `FILES` + `SUITES` |

未改动：`SCHEMA_VERSION`（仍为 `2`，按要求）；`src/import/recompute.ts` 的预算计算逻辑（原已正确，仅新增测试证明）。

---

## 2. 修复前根因（pre-fix root cause）

**A. 币种切换器溢出（核心 bug）**
- 旧实现把「月度预算」标题与币种 `Segmented` 放在同一行 `flexDirection:'row' + justifyContent:'space-between'`。标题占左、切换器占右，切换器被拉伸到远超卡片右边缘（实测约 x=1221 vs 卡片边 x=1164），在 360–430dp 小屏上整体超出屏幕宽度。
- 旧 `Segmented` 没有约束自身宽度，子项也未被 `flexShrink`/`minWidth:0` 保护，长文本 label 进一步撑宽。

**B. 本次新发现的阻断性编译错误（已修）**
- `src/version.ts` 的 `RELEASE_NOTES` 模板字符串在第 36 行已用反引号正常闭合，但**旧版发布说明文本（约第 37–53 行，来自早期双币种 V1.2.0 工作）被错误地留在闭合反引号之后**，且第 53 行还有一个悬空反引号 → 触发 `TS1127 Invalid character` / `TS1160 Unterminated template literal`（`tsc` 直接报错，阻断构建）。
- 修复：删除闭合反引号后的陈旧文本，使 `RELEASE_NOTES` 仅含 V1.2.3 内容。

**C. 测试文件类型错误（已修）**
- `__phase8_tests.ts` 误用 `'recharge'/'withdraw'/'fx'` 作为 `transactionNature` 值，而该联合类型仅含 `'normal'|'investment'|'transfer'|'repayment'|'refund'|'settlement'`。按适配器真实建模改为：`recharge → income + nature 'transfer' + affectsBudget:false`、`withdraw → transfer + affectsBudget:false`、`fx → settlement + affectsBudget:false`（与支付宝/微信/TNG 适配器一致）。

---

## 3. 卡片组件结构（`BudgetCard`，FinanceScreen.tsx:890）

信息层级（高→低，对应需求 §I）：
1. **标题** `月度预算`（或 `月度预算 · ¥` / `· RM`，仅在概览页复用、预算页不带币种）
2. **币种切换器** `Segmented`（仅 `showSwitch` 时显示；响应式内联/堆叠，见 §4）
3. **本月剩余** 小号次级文字（`labelMedium`，`theme.onSurfaceVariant`）
4. **剩余金额** 卡片内最大字号（`fontSize = round(30 * fontScale)`，`fontWeight 600`，`adjustsFontSizeToFit`，单行不折行）
5. **进度条** `ProgressIndicator`（见 §4 变色规则）
6. **已使用** + **百分比**（右对齐，`flexShrink:1` 防重叠）
7. **总预算** + **调整入口**（`TouchableOpacity` 文字按钮 `调整预算 ›` / `设置预算 ›`，`minHeight:44`，右下对齐）

特殊态：
- **超支**：显示「已超支」红字 + 红色剩余金额（`remain<0`）。
- **未设置预算**：显示「本月尚未设置 CNY/MYR 预算」+ 文字按钮「设置预算 ›」（`minHeight:44`），点击展开 `TextField`（自动带 `¥ `/`RM ` 前缀）+ 保存。
- 编辑行 `keyboardShouldPersistTaps="handled"`（见主 ScrollView）保证保存/取消不被键盘遮挡。

视觉（§VII）：`Surface level={0}`，`borderRadius:24`、`padding:16`、无厚边框/无堆叠重 Surface；颜色全部走主题 token，深色模式由主题独立深色面提供（非复用浅色玻璃）。

---

## 4. 响应式规则

| 条件 | 标题 | 切换器 |
|---|---|---|
| `winW < 430dp`（360/375/390/393/412） | 独占一行 | 占满 100% 宽度，CNY/RM 各 50%；`marginTop:10` |
| `winW ≥ 430dp`（430 及以上） | 与切换器同一行（`row + space-between`） | 紧凑内联 `width:150` |

约束（保证永不溢出，§V）：
- 卡片 `padding:16`；外宽与切换器宽度均受父容器约束。
- `Segmented` 自身 `width:'100%'`，子项 `flex:1, minWidth:0, flexShrink:1` → 子项永不溢出，长 label `numberOfLines:1` 省略而非撑宽。
- 内联变体宽度硬上限 `150dp`，远小于任何卡片宽度。
- 大字号 `Math.round(30 * PixelRatio.getFontScale())` 且 `adjustsFontSizeToFit` → 符号与数字永不分离；长金额缩放而非换行。
- 概览页两条币种卡片各自独立渲染，互不挤压。

字体/主题缩放：金额、标签均随系统 `PixelRatio.getFontScale()` 放大（含 130% / 大字体场景）。

---

## 5. 双币种计算正确性（结果）

预算扣减规则（`recompute.ts:53`）：**仅当** `affectsBudget !== false` **且** `type ∈ {expense, refund}` 时计入；bucket = `budgetCurrency ?? 原币种`，**两币种永不混合**。

| 场景 | 结果 | 测试依据 |
|---|---|---|
| CNY 消费 → 仅扣 CNY 预算，不污染 MYR | ✓ | Phase 8 #1 |
| MYR 消费 → 仅扣 MYR 预算 | ✓ | Phase 8 #1/#3 |
| 支付宝普通 → CNY 预算 | ✓ | Phase 8 #2 |
| TNG 普通 → MYR 预算 | ✓ | Phase 8 #3 |
| 理财/余额宝/蚂蚁财富（nature=investment）→ 不扣 | ✓ | Phase 8 #4 / Phase 6 / Phase 7 |
| TNG Reload（transfer）→ 不扣 | ✓ | Phase 8 #5 |
| 转账/还款/充值/提现/换汇本金 → 不扣 | ✓ | Phase 8 #6 |
| 退款 → 冲减对应币种 | ✓ | Phase 8 #7 |
| 跨币种信用卡：MYR 消费 RM100 仅扣 MYR；CNY 负债 ¥168 只计一次 | ✓ | Phase 8 #9 / Phase 7 |
| 整数最小单位（sen/fen），无浮点 | ✓ | Phase 8 #10 |
| 0 / 未设置预算 → 不除零（pct=0） | ✓ | Phase 8 #11 |
| 阈值：50% 正常 / 85% 警告 / 150% 超支（pct 封顶 100，真实 150%） | ✓ | Phase 8 #12 |

---

## 6. 各尺寸测试分析（需求 §XII）

响应式由 `useWindowDimensions().width` 在运行时决定，下表为**按尺寸的逻辑保证**（真机逐 dp 视觉确认需待构建，用户已要求暂缓）：

| 宽度(dp) | 语言 | 切换器行为 | 溢出 | 备注 |
|---|---|---|---|---|
| 360 | zh/en | 堆叠，100% 宽，50/50 | 否 | 最小屏，标题独占行 |
| 375 | zh/en | 堆叠，100% 宽，50/50 | 否 | iPhone 基准 |
| 390 | zh/en | 堆叠，100% 宽，50/50 | 否 | |
| 393 | zh/en | 堆叠，100% 宽，50/50 | 否 | Pixel 基准 |
| 412 | zh/en | 堆叠，100% 宽，50/50 | 否 | |
| 430 | zh/en | 内联（宽 150），标题同行 | 否 | 断点临界，仍不溢出 |

- **light / dark**：颜色走主题 token，`Surface level={0}` 在深色下由主题独立深色面渲染；进度条/文字/切换器配色在两种模式均满足对比度。
- **字体 100% / 130% / 大字体**：剩余金额 `adjustsFontSizeToFit` + `fontScale` 缩放，长金额（如 `RM 1,954.50`）缩放而非折行；`已使用`/`总预算` 行 `flexShrink:1` 防重叠，窄屏不出现横向滚动。
- **进度条变色**：`<80%` 主题色、`≥80%` 警告橙、`≥100%` 错误红；`>100%` 仅进度条封顶 100%，文字显示真实百分比（如 125%）。
- **静态保证**：切换器在窄屏 = 父宽 100%（永不越界）；宽屏 = 硬上限 150dp（远小于卡片）。故**卡片内无横向滚动**，且切换器永不超出屏幕，由布局约束在数学上保证。

---

## 7. 版本同步位置（需求 §XI）

| 位置 | 值 |
|---|---|
| `src/version.ts` | `MAJOR.MINO.PATCH = 1.2.3`；`VERSION_NAME="1.2.3"`；`DISPLAY_VERSION="V1.2.3"`；`VERSION_CODE=10203`；`BUILD_DATE="2026-08-29"`；`RELEASE_NOTES` 重写 |
| `package.json` | `"version": "1.2.3"` |
| `app.json` | `"version": "1.2.3"`，`android.versionCode: 10203` |
| `android/app/build.gradle` | `versionCode 10203` / `versionName "1.2.3"` |
| `SettingsScreen.tsx` | 导入并显示 `DISPLAY_VERSION`（关于副标题）、`VERSION_CODE`（构建号）、`BUILD_DATE`（更新日期）、`RELEASE_NOTES`（更新内容弹窗） |
| `store.ts`（导出/备份元数据） | `appVersion: VERSION_NAME` / `schemaVersion: SCHEMA_VERSION`（存档内嵌版本） |
| `SCHEMA_VERSION`（`src/types.ts:12`） | **保持 `2` 未改动** |

发布说明（中文）：重新设计双币种月度预算卡片，突出"剩余预算"，优化财务分页与小屏幕响应式布局，修复 CNY/MYR 切换器溢出问题。

---

## 8. 自动化测试结果

**TypeScript（`tsc --noEmit -p tsconfig.json`）**：**0 errors**（修复 version.ts / __phase8_tests.ts 后全绿）。

**导入测试 runner（`scripts/import-test-runner.js`）**：
| Suite | 结果 |
|---|---|
| Phase 1 模型/检测/迁移 | 50 passed |
| Phase 2 编码/支付宝 CSV | 55 passed |
| Phase 3 微信 XLSX | 39 passed |
| Phase 4 TNG PDF | 46 passed |
| Phase 5 标准化/匹配/统一 | 47 passed |
| Phase 6 ImportService/重算 | 160 passed |
| Phase 7 双币种预算 | 30 passed |
| **Phase 8 双币种规则（新增）** | **26 passed, 0 failed** |
| UI（状态栏/Tasks 表单） | 23 passed |
| **E2E 真实文件预览** | 7 passed, **1 failed（环境性）** |

> E2E 唯一红色：`COMBINED: both CNY and MYR budgets populated from real data`。原因：**TNG PDF 夹具加密且 `/tmp/tng_text.txt` 不存在**（运行环境无解密文本），夹具集只有 CNY 源（支付宝/微信），故 MYR 预算无数据可填充。**与本次预算卡片改动无关**，属环境性失败。

**通知测试 runner（`scripts/notify-test-runner.js`）**：**ALL SUITES GREEN**（Phase 2/3/5/6 + Confirm-form 全部通过，合计 160+ 条）。

---

## 待办 / 下一步（用户确认后）

1. **视觉确认**：在真机/Pixel 9 Pro（序列号 `46201FDAP007FD`）按 §6 逐尺寸（360/375/390/393/412/430 × zh/en × light/dark × 字体 100/130/大）核对 `BudgetCard` 与切换器。
2. **构建 V1.2.3**：确认无误后，清理构建并生成签名 Release APK（`versionCode 10203` / keystore `android-release-key.jks` alias `lifeworkbench`），ADB 安装验证。
3. **APK 当前状态**：**未构建、未安装**（严格遵循"先确认视觉与测试再构建"）。
