# V3.0.1 发布闭环 — 状态与阻塞报告

> 日期：2026-09-03
> 指令：继续完成 APK 验收 + Pixel 覆盖安装回归，版本号 bump 为 3.0.1
> 结论：**版本号 3.0.1 已在本地落地，但 APK 验收与 Pixel 回归均无法执行（3 个独立阻塞，全部在用户侧，未伪造任何构建/验签/设备结果）。**

---

## 一、已完成（本地，已提交 `3da1300`）

版本号 3.0.1 已 apply，仅改版本相关 3 文件，未碰业务源码、未改 SCHEMA_VERSION：

| 文件 | 改动 |
|---|---|
| `src/version.ts` | `APP_VERSION = {MAJOR:3, MINOR:0, PATCH:1}`；`RELEASE_NOTES` 顶部加 V3.0.1 段 |
| `android/app/build.gradle` | `versionCode 21416 → 30001`；`versionName "2.14.16" → "3.0.1"` |
| `CHANGELOG.md` | 顶部加 `## V3.0.1` 段 |

内容等同已验证的 V2.14.16 安全加固（CSPRNG / STRICT 安全存储 / 安全迁移 / 可恢复云错误 / 12 测试 + 门禁 / 两处缺陷修复 / Release 签名门禁），仅版本号变更。**SCHEMA_VERSION 未改，无 DB 迁移。**

TnG/导入适配器改动仍 **未提交、未暂存**，未混入。

---

## 二、APK 验收 — ⛔ 未执行（阻塞）

无法产出 APK，原因如下（均已用真实命令核实，**非推测**）：

### 阻塞 A：本机 git 凭据（PAT）仅有 `repo` scope，缺 `workflow`
- `git push origin main` 实推被拒：
  ```
  ! [remote rejected] main -> main
    (refusing to allow a Personal Access Token to create or update workflow
     `.github/workflows/release-android.yml` without `workflow` scope)
  ```
- API 核实：`GET /user` 返回 `x-oauth-scopes: repo`（**无 `workflow`**）。
- 说明：此前 `git push --dry-run` 显示 `9f8ee61..f0cbeb2 main -> main` 是**假成功**——GitHub dry-run 不跑服务端 scope hook，不能据此判断可推。
- 后果：CI 工作流（`.github/workflows/release-android.yml`，提交 `743f8e3`+`f0cbeb2`）**无法推送** → GitHub Actions 上不存在该工作流 → 无法在云端构建签名 APK。

### 阻塞 B：仓库 Actions Secrets 为空
- API 核实：`GET /repos/LukaXu1997/life-workbench-native/actions/secrets` → `secret names: []`。
- 4 个必需 secret 均未配置：`MYAPP_RELEASE_STORE_FILE_B64` / `MYAPP_RELEASE_KEY_ALIAS` / `MYAPP_RELEASE_STORE_PASSWORD` / `MYAPP_RELEASE_KEY_PASSWORD`。
- 后果：即便工作流推上去了，`assembleRelease` 会在 keystore 解码步按设计 `throw`（Release 缺正式签名参数即失败），仍无法出包。

### 阻塞 C：本机无 Android 工具链
- `java` 缺失、`ANDROID_HOME` 路径不存在、`apksigner` 缺失、`gh` 未安装。
- 本地无法 `assembleRelease`，且**无 keystore**（按指令不得新生成，须用历史正式 keystore）。

**综上：APK 根本不存在，以下项均无从验收（不虚构）**：applicationId / versionName / versionCode / minSdk / targetSdk / 签名有效性 / 证书是否 Debug / 证书 SHA-256 / Manifest 新增 exported·权限 / APK SHA-256 摘要。

---

## 三、Pixel 覆盖安装回归 — ⛔ 未执行（阻塞）

- 设备 `46201FDAP007FD` **离线**：`adb devices`（含 `kill-server`+`start-server` 重启）返回空列表。
- 无任何已装版本 / 包名可确认，更无法覆盖安装（且禁止先卸载）。
- 第 7 节全部回归项（覆盖升级、启动无崩溃、数据保留、应用锁、隐藏最近任务、历史备份解密、新备份、通知权限、提醒创建、通知记账入口、无障碍开关、快捷/分享入口、中英/亮暗模式）**均无法执行，未伪造**。

---

## 四、待你处理（之后我立即继续）

1. **给本环境 git PAT 加 `workflow` scope**
   - 若你已在 GitHub 给该 token 加了 scope，但本机 credential store 仍是旧 token，请更新存储凭据（`git credential reject` 后重输，或换带 `workflow` 的新 PAT）。
   - 验证：`git push origin main` 不再报 workflow scope 错误。
2. **配置 4 个仓库 Actions Secrets**（仅名称，我绝不读取/生成值）
   - 须使用**历史正式 keystore**（同一把密钥），否则旧版无法覆盖升级；未经你授权我不生成新 keystore。
3. **重连 Pixel `46201FDAP007FD`** 到本机（USB 调试），确认 `adb devices` 可见。

三项就绪后我将：push（工作流 + 3.0.1 bump）→ `workflow_dispatch` 触发构建 → 下载 APK → 用 androguard/aapt 验收（applicationId `com.luka.lifeworkbench`、versionName `3.0.1`、versionCode `30001`、minSdk 24 / targetSdk 35、签名非 Debug、证书 SHA-256 与历史一致、Manifest 无新增高风险项、记录 APK SHA-256）→ 仅在验签通过后对 Pixel **覆盖安装（不卸载）**执行第 7 节全量回归 → 打 `v3.0.1` tag 并推送。

---

## 五、当前本地 git 状态（未 push）

- `origin/main` = `9f8ee61`（v2.14.16，已上线）
- 本地 `main` = `3da1300`，领先 origin 3 个提交（均未 push）：
  - `743f8e3` ci: add signed Android release workflow
  - `f0cbeb2` ci: harden release workflow gates
  - `3da1300` chore: bump version to 3.0.1
- `v2.14.16` tag 已在 origin，未动；`v3.0.1` tag 尚未创建。
- 工作区未提交改动（保持原样）：`scripts/import-test-runner.js`、`src/import/*`、`src/screens/ImportFlowModal.tsx`、`src/import/__phase9_tests.ts`、`src/import/adapters/myrEwalletCsv.ts`、`.tmptest_tng/` 及两份报告 md。
