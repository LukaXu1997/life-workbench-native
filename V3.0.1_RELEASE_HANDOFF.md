# V3.0.1 发布交接（签名已恢复）

## 关键结论
- 原 release keystore 密码已找回：`Qf248VmKoCrmoUX6Gzo7ZNcy`（store 与 key 相同）。
- 已用 JDK17 keytool 验证：alias=`lifeworkbench`，证书 SHA-256 = `14:80:48:56:2D:94:25:7D:42:C6:89:9C:FB:04:0B:BE:BE:F1:9E:D4:F7:F1:55:24:41:78:52:66:E0:DC:BC:EB`，与历史正式证书 `148048562d94257d42c6899cfb040bbebef19ed4f7f1552441785266e0dcbceb` **逐字节一致**。
- ⇒ 原签名身份完整保留，**无需生成新 keystore、无需用户迁移**，现有安装可原地升级到 V3.0.1。
- 密码已存入 macOS 钥匙串（service=`life-workbench release keystore`，account=`lifeworkbench`）；之前失败的新 keystore 尝试已清理。

## 4 个 GitHub Actions Secrets（Settings → Secrets and variables → Actions）
1. `MYAPP_RELEASE_STORE_FILE_B64` = 原 keystore 的 base64
   - 取值命令：`pbcopy < /Users/Luka/WorkBuddy/_release_secrets/android-release-key.b64`（已生成，600 权限，约 3593 字节）
2. `MYAPP_RELEASE_KEY_ALIAS` = `lifeworkbench`
3. `MYAPP_RELEASE_STORE_PASSWORD` = `Qf248VmKoCrmoUX6Gzo7ZNcy`
4. `MYAPP_RELEASE_KEY_PASSWORD` = `Qf248VmKoCrmoUX6Gzo7ZNcy`

> 以后若再忘密码，本机取回：`security find-generic-password -s "life-workbench release keystore" -w`

## 发布 runbook（Secret 填好后由 Agent 执行）
1. 给本机 PAT 增加 `workflow` scope 并更新 git credential（`git credential reject` / `git credential approve`），使能推送 CI 工作流文件。
2. `git push origin main` —— 推送 3 个本地提交：`743f8e3`(加 CI)、`f0cbeb2`(CI 加固)、`3da1300`(bump 3.0.1)。
   - ⚠️ 工作区另有未提交的 import/TnG 改动（`src/import/*`、`src/import/adapters/myrEwalletCsv.ts`、`.tmptest_tng/` 等），**不纳入本次发布**，保持现状不提交。
3. 触发 `release-android.yml`（`workflow_dispatch` 或推送 `v3.0.1` tag）。
4. 下载 APK 验收：`applicationId=com.luka.lifeworkbench`、`versionName=3.0.1`、`versionCode=30001`、签名非 `androiddebugkey`、证书 SHA-256=`14804856…e0dcbceb`。
5. Pixel（46201FDAP007FD）覆盖安装回归（不卸载，验证数据保留 + Supabase 恢复）。
6. 打 tag `v3.0.1` 并推送。

## 当前阻塞（仅用户侧）
- ❌ 本机 PAT 缺 `workflow` scope → 无法 push CI 工作流文件。
- ❌ 4 个 Actions Secrets 未填。
完成这两步即可继续。
